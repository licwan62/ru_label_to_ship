# 可迁移匹配规则包

每个批次可在根目录放置 `fuzzy_matches.json`。程序自动加载它；也可通过
`--fuzzy-matches <文件>` 指定共享规则包。

```json
{
  "材质": {
    "PEVA": "单层PEVA"
  },
  "默认值": {
    "材质": "单层PEVA"
  },
  "货号尾段尺码": true,
  "材质括号说明": true,
  "货号": {
    "LADA-2121-LINK-S": {
      "发货尺码": "S",
      "材质": "PEVA"
    }
  }
}
```

- `材质`：键是原始材质，值是最终分组材质，必须完整匹配。
- `默认值.材质`：仅当信息表材质为空时补入该材质。
- `货号尾段尺码`：启用后，仅对信息表和显式货号规则均未命中的货号，尝试取最后一个下划线后的合法尺码代码，例如 `abc_def_S → S`。
- `材质括号说明`：启用后，仅剥离信息表材质**末尾**的完整中英文括号说明用于分组，例如 `单层PEVA（灰色无耳）→ 单层PEVA`；原始材质仍保留在审计表，`对应依据`记录剥离前后的值。剥离后的值才参与 `材质` 别名映射。只处理信息表原始材质，不影响货号规则或默认值中显式写的材质。
- `货号`：键是完整货号；每条规则必须且只能包含非空的 `发货尺码` 和 `材质`。
- 货号规则可处理信息表缺少货号、材质缺失或尺码带说明文字的情况。
- 规则不会按相似文本、标题或货号片段猜测。
- 单笔 `manual_overrides.csv` 的有效人工确认优先于规则包。
- `manual_overrides.csv` 中尚未填写的模板行只是占位符，不会压过规则包。

新增或修改规则后先运行：

```powershell
python -m src.cli audit --batch sample/<批次>          # 固定格式批次
python -m src.cli audit --input-dir input/<批次>       # 扁平输入目录批次
```

确认审计无阻断项后再运行 `build`。
