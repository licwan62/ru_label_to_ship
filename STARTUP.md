# Ozon 标签转发货包：项目启动文档

> 最新目录约定：见 [README.md](README.md#安装与运行)。以下 `data/9.10` 是本文档最初编写时的案例路径，现已固定格式搬到 `sample/9.10`（结构不变：`input/labels/`、`input/发货单/`、`input/信息表/`；手动映射为批次根目录 `manual_overrides.csv`；交付目录为 `output/发货包/`）；日常新批次改用 `input/<批次>` + `artifacts/<批次>` 的扁平输入目录格式。交付包仅包含案例中的分组 PDF、逐单核对清单和分组说明。完整匹配表、待确认、跳过清单和统计摘要置于包外审计目录。以下早期工作流中的包内辅助文件约定以本条和 README 为准。

## 1. 建设目标

本项目把 Ozon 原始标签 PDF、发货单 CSV 和商品信息表 CSV 合并为可核对、可追溯的发货数据，并按“材质 + 发货尺码”拆分标签 PDF，生成可直接交付仓库的发货包。

本阶段只明确数据模型、匹配规则、验证规则、目录约定和实施顺序。后续实现不得覆盖原始标签、发货单或信息表。

## 2. 研究实体与样例

### 2.1 本期输入

| 数据角色 | 路径 | 当前规模 | 用途 |
| --- | --- | ---: | --- |
| 原始标签 | `data/9.10/input/labels/9.10发货.pdf` | 55 页 | 提供每个包裹的原始标签页、完整发货号码、配送服务等标签元素 |
| 发货单 | `data/9.10/input/发货单/发货商品信息9.10.csv` | 55 行 | 建立“完整发货号码 → 货号”的桥接关系 |
| 商品信息表 | `data/9.10/input/信息表/Ozon上架链接汇总_含发货尺码及无尺码备注.csv` | 134 行 | 根据货号取得发货尺码、材质及相关备注 |

### 2.2 输出样例

`9.7发货包/` 是目标结构参考，包含：

- 按材质划分的目录；
- 每个材质目录下按发货尺码合并的 PDF；
- `逐单核对清单.csv`；
- 分组说明和特殊材质备注。

样例中的标签保持原始页面尺寸约 58 × 40 mm，无新增封面。文件名包含发货尺码、材质和页数。

## 3. 已确认的数据事实

### 3.1 标签页面结构

9.10 标签 PDF 共 55 页，每页尺寸为 164.25 × 113.25 pt，约等于 57.94 × 39.95 mm。每页是一张独立标签，主要元素包括：

| 元素 | 第一页示例 | 处理方式 |
| --- | --- | --- |
| Ozon 标识号 | `OZON GLOBAL: 4103119` | 原样提取并保留，暂不参与关联 |
| 完整发货号码 | `40552532-0418-1` | 标签与发货单之间的唯一关联键 |
| 分拣标识 | `C / ПВЗ` | 原样提取并保留，等待业务验证 |
| 俄文收货地址 | 第一页为 Krasnodar Krai 地址 | 原样保留；允许提取文本，但不得重排或修改标签 |
| 中国地址 | `China, Yiwu, Beiyuan Subdistrict postal, 10, 1005` | 原样提取并保留，等待业务验证 |
| 配送服务 | `GUOO Economy Budget` | 与发货单的配送服务交叉验证 |
| 二维码 | 页面右侧二维码 | 原始 PDF 中保留；二维码解码结果可作为辅助字段，不作为首选关联键 |

注意：`40552532-0418-1` 不是商品货号，它在发货单中的字段是“发货号码”。第一页对应的商品货号是 `20260803013`。正式关联链路必须是：

```text
标签页完整发货号码
  → 发货单.发货号码
  → 发货单.货号
  → 信息表.货号
  → 信息表.发货尺码、信息表.材质
```

### 3.2 当前匹配审计

- 标签 PDF：55 页，每页均提取到且只提取到 1 个完整发货号码，共 55 个唯一号码。
- 发货单：55 行、55 个唯一发货号码、数量合计 55。
- PDF 与发货单的完整发货号码集合完全一致，无缺页、无多余记录。
- PDF 页序与发货单行序不同：只有第一页位置一致。因此禁止按位置关联。
- 配送服务核对一致：`GUOO Economy Budget` 23 条，`GUOO Economy Small` 32 条，无差异。
- 发货单包含 28 个不同货号。
- 53 条发货记录能通过货号唯一匹配信息表，并取得非空的发货尺码和材质。
- 当前没有出现“同一货号匹配信息表多行”的歧义。

### 3.3 当前阻断项

以下货号无法在 9.10 信息表中精确找到，正式发货包不得自动归组：

| 完整发货号码 | 货号 | 数量 | 处理要求 |
| --- | --- | ---: | --- |
| `0176606413-0004-1` | `VESTA-2M` | 1 | 需要补充信息表记录或人工确认发货尺码、材质 |
| `0117816836-0056-1` | `0017573` | 1 | 需要补充信息表记录或人工确认发货尺码、材质 |

另有一个已匹配货号需要规范化确认：

| 货号 | 信息表发货尺码 | 信息表材质 | 风险 |
| --- | --- | --- | --- |
| `LADA-2121-LINK-S` | `S(应该发ys，但是没有就发s)` | `PEVA` | 发货尺码混入说明文字，材质命名也与“单层PEVA”不同；生成目录前必须确认标准分组值 |

系统不得从商品标题、相似货号或历史批次静默猜测这三个问题的答案。人工确认必须以覆盖表的形式留痕。

## 4. 工作流设计

### 阶段 A：标签 PDF 解析

逐页处理原始 PDF，输出 `labels_parsed.csv`。每个 CSV 行对应一个原始 PDF 页，不改变原始页顺序。

建议字段：

| 字段 | 含义 |
| --- | --- |
| `原标签页码` | 从 1 开始的页码 |
| `完整发货号码` | 标签上的完整号码，核心关联键 |
| `尾四位` | 仅供人工查看，不得用于关联 |
| `Ozon Global号` | `OZON GLOBAL` 后的数字 |
| `配送服务` | 如 `GUOO Economy Budget` 或 `GUOO Economy Small` |
| `分拣标识` | 如 `C / ПВЗ`、`B` |
| `收货地址原文` | 页面上的俄文地址文本 |
| `中国地址原文` | 页面上的中国地址文本 |
| `二维码内容` | 可选的二维码解码结果 |
| `解析状态` | `通过`、`缺少发货号码`、`多个发货号码`、`配送服务未识别` 等 |
| `原始文本` | 页面文字提取结果，用于审计 |

解析规则：

1. 用正则识别完整发货号码，格式为 `数字-4位数字-1位数字`，但仍保存原始文本。
2. 每页必须且只能识别出一个完整发货号码，否则标记为阻断。
3. 原标签页的图形、二维码、字体和尺寸不做重绘。拆分时直接复制原 PDF 页对象。
4. 配送服务、地址、分拣标识等字段先保留，再与发货单交叉验证；未知元素不得丢弃。

### 阶段 B：发货单标准化

读取发货单后只做非破坏性标准化：

- 去除字段两端空白；
- 把发货号码、货号、标签尾号视为文本，保留前导零；
- 数量解析为整数；
- 保留商品名称和照片公式作为源字段；
- 不按 `NO` 或文件行序与 PDF 关联。

输出工作表 `shipment_normalized.csv`，原文件保持不变。

### 阶段 C：两段式匹配

第一段使用 `labels_parsed.完整发货号码 = shipment_normalized.发货号码` 精确匹配。

第二段使用 `shipment_normalized.货号 = 信息表.货号` 精确匹配，并取得：

- `发货尺码`；
- `材质`；
- `无尺码备注（车型/尺寸）`，用于辅助人工复核；
- 信息表序号或来源工作表，作为追溯信息。

匹配前只允许去除两端空白，不允许自动删除货号中的前导零、连字符或其他字符。若业务需要别名，必须通过独立的 `manual_overrides.csv` 显式维护。

### 阶段 D：验证与人工覆盖

建议人工覆盖表结构：

| 字段 | 说明 |
| --- | --- |
| `完整发货号码` | 优先使用，确保覆盖只作用于一单 |
| `货号` | 辅助核对 |
| `确认发货尺码` | 人工确认后的标准尺码 |
| `确认材质` | 人工确认后的标准材质 |
| `确认人` | 留痕 |
| `确认日期` | ISO 日期 |
| `确认依据` | 简短说明 |

覆盖优先级：单笔人工覆盖 > 信息表精确匹配。任何覆盖都必须出现在最终核对清单的“对应依据”和“状态”中。

进入 PDF 分组前必须满足：

1. 标签页数 = 标签解析行数 = 发货单行数 = 完整匹配表行数。
2. 每个完整发货号码在 PDF、发货单、完整匹配表中各出现一次。
3. 每条记录都有唯一且非空的标准发货尺码、标准材质。
4. 标签配送服务与发货单配送服务一致。
5. 数量合计与标签页数一致。当前业务是一件一标签；若以后出现数量大于 1，必须先定义“复制标签”还是“单页多件”，不得默认复制。
6. 未匹配、重复匹配、冲突和未确认覆盖项均为阻断状态。

### 阶段 E：生成完整匹配表

输出 `完整匹配表.csv`，建议字段顺序：

```text
原标签页码,完整发货号码,尾四位,Ozon Global号,标签配送服务,
发货单配送服务,货号,商品,数量,信息表序号,来源工作表,
原始发货尺码,原始材质,标准发货尺码,标准材质,
无尺码备注,对应依据,匹配状态,验证信息
```

状态至少区分：

- `自动匹配通过`
- `人工确认通过`
- `发货号码未匹配`
- `货号未匹配`
- `货号重复匹配`
- `尺码缺失`
- `材质缺失`
- `配送服务不一致`
- `等待人工确认`

### 阶段 F：生成发货包

仅使用“自动匹配通过”或“人工确认通过”的记录。按标准材质建目录，再按标准发货尺码分组。同一材质、同一尺码的标签页合并到一个 PDF。

推荐目录：

```text
data/9.10/output/发货包/
├── 完整匹配表.csv
├── 逐单核对清单.csv
├── 先看这里_分组说明.txt
├── <标准材质>/
│   ├── 发<标准尺码>码_<标准材质>_<页数>张.pdf
│   └── ...
└── 待确认/
    └── 待确认清单.csv
```

PDF 生成规则：

- 直接从原 PDF 复制页面，保持约 58 × 40 mm 的页面尺寸和全部内容；
- 不缩放、不裁剪、不旋转、不栅格化、不添加封面；
- 组内默认按原标签页码升序，确保可重复生成；
- 一个原始页面只能进入一个最终分组文件；
- 文件名中的页数必须等于实际 PDF 页数；
- 材质和尺码须经过文件名非法字符清理，但 CSV 中保留原值和标准值；
- 待确认记录不混入可发货 PDF，可单独输出清单供确认。

`逐单核对清单.csv` 兼容 9.7 样例，至少保留：

```text
分组文件,文件内页码,发货尺码,材质,完整发货号码,尾四位,
货号,数量,原标签页码,对应依据,状态,发货备注
```

## 5. 输出后的完整性检查

生成结束后执行以下独立检查：

- 汇总所有分组 PDF 的页数，必须等于已通过记录数；
- 对每个分组 PDF 重新读取页数和页面尺寸；
- 从输出 PDF 再提取完整发货号码，与完整匹配表逐条核对；
- 确认没有重复号码、遗漏号码或跨材质/尺码混组；
- 确认 `逐单核对清单.csv` 中的文件内页码与实际 PDF 顺序一致；
- 确认原始 PDF、发货单、信息表的文件哈希或修改时间未被改变；
- 输出统计摘要：输入页数、通过数、待确认数、各材质/尺码页数、配送服务分布。

## 6. 当前 9.10 的预估分组

以下统计仅包含当前能唯一匹配信息表的 53 条记录，不能直接视为最终发货结果：

| 材质原值 | 发货尺码原值 | 标签数 |
| --- | --- | ---: |
| 单层PEVA | 2XXXL525 | 8 |
| 单层PEVA | L | 18 |
| 单层PEVA | M | 15 |
| 单层PEVA | S | 1 |
| 单层PEVA | YL | 2 |
| 单层PEVA | YM | 1 |
| 单层PEVA | YXL | 2 |
| PEVA | S（含“应该发ys，但是没有就发s”说明） | 6 |

剩余 2 条为未匹配货号。确认 `VESTA-2M`、`0017573` 以及 `LADA-2121-LINK-S` 的标准分组值后，最终数量应回到 55 页。

## 7. 建议实现结构

```text
src/
├── extract_labels.py      # PDF 逐页解析
├── load_sources.py        # CSV 读取、编码和字段标准化
├── match_records.py       # 两段式精确匹配及覆盖应用
├── validate_records.py    # 阻断项与完整性检查
├── build_package.py       # PDF 拆分、合并及清单生成
└── cli.py                 # 统一命令入口
tests/
├── test_extract_labels.py
├── test_matching.py
├── test_validation.py
└── test_build_package.py
```

推荐统一入口：

```powershell
python -m src.cli build --batch data/9.10
```

建议支持两个模式：

- `audit`：只解析、匹配并生成完整匹配表和待确认项，不生成发货 PDF；
- `build`：全部验证通过后生成正式发货包。

PDF 可使用 `pypdf` 做页面复制与合并，文本提取使用 `pypdf` 或 `pdfplumber`。二维码识别是增强项，不应阻塞基础链路。CSV 应使用支持引号和字段内换行的标准解析器，统一以 UTF-8 BOM 输出，方便中文 Windows Excel 打开。

## 8. 实施顺序与验收标准

### 里程碑 1：解析和审计

- 完成标签逐页解析；
- 完成两段式匹配；
- 输出完整匹配表和待确认清单；
- 对 9.10 得出与本文件一致的 55 页、55 单、2 个未匹配货号结论。

### 里程碑 2：人工确认闭环

- 建立 `manual_overrides.csv`；
- 解决当前三个标准化/匹配问题；
- 所有记录达到可分组状态。

### 里程碑 3：发货包生成

- 按材质和尺码生成 PDF；
- 输出逐单核对清单与分组说明；
- 完成页面数量、号码集合、页面尺寸和组内顺序复核。

最终验收要求：55 个原始标签页全部且仅出现一次；每一页能追溯到发货单和信息表或人工确认记录；同一 PDF 内材质、尺码一致；所有未确认项都被阻断而不是自动猜测。
